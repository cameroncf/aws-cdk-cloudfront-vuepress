{
  "name": "awscdkcloudfrontvuepress",
  "version": "0.0.1",
  "scripts": {
    "aws:build": "tsc",
    "aws:clean": "rm -rf dist && rm -rf cdk.out",
    "www:build": "vuepress build www",
    "www:clean": "rm -rf www/.vuepress/dist",
    "www:dev": "vuepress dev www",
    "cdk": "cdk",
    "build": "yarn www:build && yarn aws:build",
    "clean": "yarn www:clean && yarn aws:clean",
    "dry-run": "yarn clean && yarn build && yarn test && yarn cdk synth -q",
    "test": "jest"
  },
  "peerDependencies": {
    "aws-cdk-lib": "2.0.0-rc.27",
    "constructs": "^10.0.0"
  },
  "devDependencies": {
    "@aws-cdk/assert": "2.0.0-rc.27",
    "@types/jest": "^26.0.10",
    "@types/node": "10.17.27",
    "aws-cdk": "2.0.0-rc.27",
    "aws-cdk-lib": "2.0.0-rc.27",
    "constructs": "^10.0.0",
    "jest": "^26.4.2",
    "ts-jest": "^26.2.0",
    "ts-node": "^9.0.0",
    "typescript": "~3.9.7",
    "vuepress": "^2.0.0-beta.27"
  },
  "dependencies": {
    "@aws-cdk/assert": "2.0.0-rc.27",
    "cognito-at-edge": "^1.1.0",
    "constructs": "^10.0.0"
  }
}
